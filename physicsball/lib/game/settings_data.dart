import 'package:flutter/material.dart';

class AppSettings {
  // Физика
  static double gravity = 1200.0;
  static double bounce = 0.7;
  static bool collisionsEnabled = true;

  // Визуал
  static Color ballColor = Colors.cyanAccent;
  static bool randomColors = true;
}