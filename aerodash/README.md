# 🛩️ Custom Physics Plane Game

Аркадная игра на Flutter, вдохновленная классической механикой Flappy Bird, но с расширенными возможностями **кастомизации физики в реальном времени**.

## 🌟 Особенности

* **Механика «Коридора»:** Препятствия генерируются парами (сверху и снизу), создавая узкий проход для маневров.
* **Плавная физика:** Прыжки реализованы через импульс вертикальной скорости (), что дает ощущение инерции и плавности полета.
* **Динамический наклон:** Самолет реалистично наклоняет нос при наборе высоты и падении.
* **Гибкие настройки:** Специальное меню позволяет менять параметры игры «на лету».

## 🛠 Настройки физики

В игре реализован уникальный движок, параметры которого можно менять через ползунки. По умолчанию установлены оптимальные значения (сброс к ним происходит по кнопке в AppBar настроек):

| Параметр | Описание | Значение по умолчанию (50% слайдера) |
| --- | --- | --- |
| **Тяга двигателя** | Сила импульса при нажатии на экран. | **1.3** (мощный рывок) |
| **Гравитация** | Сила, притягивающая самолет к земле. | **0.65** (комфортное падение) |
| **Размер самолета** | Масштаб визуальной модели и хитбокса. | **1.5x** (крупный самолет) |
| **Скорость полета** | Скорость движения труб навстречу игроку. | **5.0** |
| **Частота спавна** | Как часто появляются новые препятствия. | **0.3** |

## 📁 Структура проекта

* `lib/game/game_engine.dart` — ядро игры: расчет скоростей, коллизий и логика "коридора".
* `lib/game/game_settings.dart` — синглтон-хранилище настроек с формулами пересчета.
* `lib/screens/game_screen.dart` — игровой процесс и рендеринг через `Stack`.
* `lib/screens/settings_screen.dart` — интерфейс редактирования параметров.
* `assets/plane.png` — кастомное изображение игрока.

## 🚀 Запуск

1. Убедитесь, что у вас установлен **Flutter SDK**.
2. Склонируйте репозиторий.
3. Добавьте изображение самолета по пути `assets/plane.png`.
4. Проверьте регистрацию ассетов в `pubspec.yaml`:
```yaml
flutter:
  assets:
    - assets/plane.png

```


5. Запустите проект:
```bash
flutter run

```



## 🎮 Управление

* **Нажатие на экран:** Прыжок (импульс вверх).
* **Главное меню:** Доступ к настройкам и логам.
* **Кнопка Сброса (в настройках):** Возвращает ползунки на 50%, устанавливая вашу любимую физику.
