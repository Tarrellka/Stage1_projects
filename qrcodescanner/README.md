# 🛡️ AI QR Scanner & Generator Pro

Интеллектуальная утилита на **Flutter** для работы с QR-кодами. Приложение не только сканирует и генерирует коды, но и использует **AI (OpenRouter API)** для анализа безопасности содержимого.

## ✨ Основные функции

* **🔍 Умный сканер**: Захватывает QR-коды и мгновенно анализирует их. Камера автоматически приостанавливается для удобного чтения вердикта.
* **🤖 AI Security Analysis**: Интеграция с нейросетями через OpenRouter. Система проверяет ссылки на фишинг и вредоносное ПО, выдавая краткий отчет на русском языке.
* **➕ Генератор QR**: Создание собственных кодов из любого текста или ссылок в реальном времени.
* **💾 Экспорт в галерею**: Возможность сохранить сгенерированный QR-код как изображение прямо на устройство.
* **📜 Умная история**: Все сканы сохраняются локально. Список обновляется мгновенно при переключении вкладок благодаря системе сигналов (`ValueNotifier`).
* **📋 Буфер обмена**: Быстрое копирование сырых данных кода или текста анализа AI.

## 🛠️ Технологический стек

* **Framework**: [Flutter](https://flutter.dev) (Dart)
* **AI Gateway**: [OpenRouter API](https://openrouter.ai) (модели Mistral / Gemini)
* **Scanning**: `mobile_scanner`
* **QR Generation**: `qr_flutter`
* **Storage**: `shared_preferences`
* **Media**: `screenshot` & `gal` (для сохранения в галерею)

---

## 🚀 Быстрый старт

### 1. Настройка окружения

Убедитесь, что зависимости установлены:

```bash
flutter pub get

```

### 2. API Ключ

Добавьте ваш ключ в `lib/services/ai_service.dart`:

```dart
static const String _apiKey = 'ВАШ_КЛЮЧ_ОТ_OPENROUTER';

```

### 3. Разрешения (Android)

Для корректного сохранения QR-кодов убедитесь, что в `AndroidManifest.xml` добавлены необходимые разрешения для записи (для старых версий Android).

---

## 📂 Архитектура проекта

Приложение следует принципу разделения ответственности (**MVC-like**):

* **`/controllers`**: Управление состоянием сканера и бизнес-логикой.
* **`/screens`**: Чистый UI (Сканер, Генератор, История).
* **`/services`**: Изолированная логика работы с сетью и локальной БД.

---

## ⚙️ Конфигурация AI

Приложение использует строго настроенный промпт для получения детерминированных ответов:

* **Temperature**: `0.1` — исключает «фантазии» нейросети.
* **System Prompt**: Фокусирует модель исключительно на анализе безопасности.

---

## 📱 Сборка

Для создания релизной версии APK:

```bash
flutter build apk --release